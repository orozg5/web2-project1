import { Button, Flex, Heading } from "@chakra-ui/react";

export const Navbar = () => {
  return (
    <Flex justify="space-between">
      <Heading>Web2 - Project1</Heading>
      <Button>Login</Button>
    </Flex>
  );
};
